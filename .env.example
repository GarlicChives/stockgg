# Copy to .env and fill the real value. .env is gitignored — never commit.
#
# Supabase anon (legacy JWT format) key for project mnseyguxiiditaybpfup.
# Required by src/utils/db.py. The DB layer talks to the db-proxy-public
# Edge Function, which hard-allowlists 9 SELECT shapes, so this key can
# ONLY read the public-safe columns enumerated in
# supabase/functions/db-proxy-public/index.ts.
SUPABASE_ANON_KEY=eyJ...
